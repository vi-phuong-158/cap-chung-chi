---
description: Các lệnh phát triển thường dùng cho project Edu-Support
---

// turbo-all

## Xem file structure
1. Xem danh sách file trong project
dir

## Mở trình duyệt test
2. Mở file index.html trong trình duyệt
start index.html

## Git commands
3. Kiểm tra trạng thái git
git status

4. Xem lịch sử commit
git log -n 5 --oneline

5. Thêm tất cả thay đổi
git add .

6. Commit với message
git commit -m "Update"

7. Push lên remote
git push

## Deploy to GitHub Pages
8. Build và deploy
git push origin main
